services:
  - type: web
    name: clinsynapsecloud
    env: docker
    plan: starter
    region: oregon

    # Your Dockerfile builds the server
    dockerfilePath: ./Dockerfile

    envVars:
      - key: PUBLIC_BASE_URL
        value: https://clinsynapsecloud.onrender.com



